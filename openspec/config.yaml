# openspec/config.yaml
# Auto-generated by sdd-init — RoyalTDN project
schema: spec-driven

context: |
  Tech stack: Python 3.10+, pandas, numpy, Alpaca Markets API (alpaca-py), VectorBT, Backtrader, TimescaleDB, Redis, Docker, GitHub Actions, Grafana.
  Architecture: Modular event-driven bot with DataFeedHandler → StrategyEngine → RiskManager → ExecutionManager (O/EMS). Inter-module communication via Redis Streams. TimescaleDB for time-series, Parquet on S3/MinIO for cold storage, Prometheus+Grafana for monitoring.
  Testing: pytest (planned). No test files exist yet — greenfield project. CI pipeline includes lint (Ruff/Flake8), type check (MyPy), unit tests (pytest), smoke backtest.
  Style: Spanish docs (roadmap), Python src will be English. asyncio-based I/O. Dotenv for secrets. Docker multi-stage builds. Conventional commits.

rules:
  proposal:
    - Include rollback plan for risky changes
    - Reference relevant roadmap phase (Fase 0-4)
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Spanish for domain descriptions, English for technical interfaces
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - Reference the modular design (ingestor → strategy → risk → O/EMS)
  tasks:
    - Group by phase (Fase 0-4), use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns in the same module
    tdd: false
    test_command: ""
  verify:
    test_command: "pytest"
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  test_command: ""
  strict_tdd: false
  test_framework: pytest
  test_layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  linter: ""
  type_checker: ""
  formatter: ""
